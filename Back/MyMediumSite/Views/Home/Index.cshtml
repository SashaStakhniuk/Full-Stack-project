@model MyMediumSite.Models.ViewModels.PersonViewModel
@{
}
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
<div class="p-3">
    <div style="text-align:center;">
        <h1>The server was started</h1>
    </div>
    @*@foreach (var person in Model.Persons)
    {
        <h5>Amount of followers: @person.MyFollowers</h5>

        <img style='width:20%;' src="data:image/jpeg;base64,@(System.Text.Encoding.ASCII.GetString(Model.Profiles.Where(x => x.ProfileId == person.ProfileId).Select(x => x.ProfilePhoto).FirstOrDefault()))" />
    }*@
</div>
